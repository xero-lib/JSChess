// the board is initialized automatically

import { makeMove } from "./index.js";

/* 
    makeMove attempts to move a piece from a2 to a3, returns true if move is successful.
    Check the README for the other possible return codes.
*/
console.log(makeMove("a2", "a3")); 